# 🎨 Evidencias — Replicate (Parte 2.7)

# Taller 2.7 — Modelos Generativos con Replicate

## Descripción
El objetivo de este taller era **probar modelos generativos** mediante la plataforma [Replicate](https://replicate.com/), que permite ejecutar modelos de **imagen, voz, texto o video** a través de API o interfaz gráfica sin necesidad de infraestructura local. Esto facilita el **prototipado rápido, despliegue público y pruebas multimodales**.

## Desarrollo
Se intentó ejecutar dos modelos generativos mediante la API de Replicate para generar contenido visual y textual. La idea era:

- Probar la generación de imágenes y texto.
- Construir un script que consuma la API.
- Evaluar latencia y calidad de los resultados.

### Estado de la ejecución
Al intentar ejecutar los modelos, la plataforma mostró el siguiente mensaje:

Error: You have insufficient credit to run this model. Go to https://replicate.com/account/billing#billing
 to purchase credit.

 
Esto indica que **la ejecución del modelo requiere créditos pagos**, por lo que no fue posible generar las imágenes o textos solicitados.

## Evidencia
- Intento de ejecución de la API (captura de error).
- Script base para consumo de API (sin resultados finales).
- Carpeta `/replicate/` incluida en el repositorio con archivos iniciales.

## Conclusión
Aunque no fue posible generar los outputs finales debido a la restricción de crédito, se documentó el **proceso de acceso, configuración y ejecución del modelo** en Replicate, cumpliendo parcialmente con los requerimientos del taller y mostrando evidencia del intento.

